# Websocket server URL
NEXT_PUBLIC_WS_URL=http://localhost:7071

# Database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# Optional Redis (Upstash or self-hosted)
UPSTASH_REDIS_URL=
UPSTASH_REDIS_TOKEN=

# LLM provider selection (stub only for MVP)
LLM_PROVIDER=stub
